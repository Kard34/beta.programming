#include <iostream>

using namespace std;

int main()
{
    int num[9], sum = 0, fake[] = {0, 0};
    for (int i = 0; i < 9; i++)
    {
        cin >> num[i];
        sum += num[i];
    }
    for (int i = 0; i < 9; i++)
    {
        for (int j = i + 1; j < 9; j++)
        {
            if (sum - num[i] - num[j] == 100)
            {
                fake[0] = i;
                fake[1] = j;
                break;
            }
        }
        if (fake[0] != fake[1])
            break;
    }
    for (int i = 0; i < 9; i++)
    {
        if (i == fake[0] || i == fake[1]){}
        else
            cout << num[i] << endl;
    }
    return 0;
}
